# Configuração do Railway - Cole estas variáveis no painel do Railway

SECRET_KEY=your-secret-key-here-change-this
DEBUG=False

# Para Railway, use * para permitir todos os hosts
ALLOWED_HOSTS=*

# URL pública do Railway (opcional - será detectada automaticamente)
RAILWAY_PUBLIC_URL=https://your-app-name.up.railway.app

# Configurações do Gunicorn
WEB_CONCURRENCY=3
TIMEOUT=120
WORKER_CONNECTIONS=1000
LOG_LEVEL=info

# IMPORTANTE:
# 1. Substitua 'your-secret-key-here-change-this' por uma chave secreta forte
# 2. O Railway configurará automaticamente DATABASE_URL quando você adicionar PostgreSQL
# 3. O PORT é configurado automaticamente pelo Railway
# 4. RAILWAY_PUBLIC_URL é opcional - o Django detecta automaticamente domínios *.railway.app
# 5. Se o erro CSRF persistir, tente definir DEBUG=True temporariamente para ver os logs